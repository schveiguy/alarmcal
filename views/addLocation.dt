extends layout
block content
  include form
  h1 Add Location

  form(method="POST", action="/performAddLocation")
    section
      - import alarmcal.db;
      - createForm!Location();
    .form-group
      button(type="submit") Submit

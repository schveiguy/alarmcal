extends layout
block content
  include form
  h1 Edit Location

  form(method="POST", action="/performEditLocation")
    input(type="hidden", name="id", value=(item.id))
    section
      - createForm(item);
    .form-group
      button(type="submit") Submit

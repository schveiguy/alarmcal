extends layout
block content
  include form
  h1 Edit Person

  form(method="POST", action="/performEditPerson")
    input(type="hidden", name="id", value=(item.id))
    section
      - createForm(item);
    .form-group
      button(type="submit") Submit

extends layout
block content
  include form
  h1 Add Person

  form(method="POST", action="/performAddPerson")
    section
      - import alarmcal.db;
      - createForm!Person();
    .form-group
      button(type="submit") Submit
